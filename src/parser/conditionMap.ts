export default {
  "==": (a: any, b: any): boolean => a === b,
  "!=": (a: any, b: any): boolean => a !== b,
  ">": (a: any, b: any): boolean => a > b,
  ">=": (a: any, b: any): boolean => a >= b,
  "<": (a: any, b: any): boolean => a < b,
  "<=": (a: any, b: any): boolean => a <= b
};
